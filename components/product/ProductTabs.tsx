'use client';import{useEffect,useState}from'react';const tabs=[['overview','Overview'],['features','Features'],['specifications','Specifications'],['oem','OEM & ODM'],['faq','FAQ'],['inquiry','Inquiry']];export function ProductTabs(){const[active,setActive]=useState('overview');useEffect(()=>{const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)setActive(e.target.id)}),{rootMargin:'-35% 0px -55%'});tabs.forEach(([id])=>{const el=document.getElementById(id);if(el)obs.observe(el)});return()=>obs.disconnect()},[]);return <nav className="sticky top-[84px] z-40 border-y border-line bg-white/95 backdrop-blur"><div className="container-page flex overflow-x-auto">{tabs.map(([id,label])=><a className={`shrink-0 border-b-2 px-5 py-4 text-sm font-semibold ${active===id?'border-brand text-brand':'border-transparent text-body'}`} href={`#${id}`} key={id}>{label}</a>)}</div></nav>}
